namespace CraveIt.API.Helpers
{
    // Enum Definition: User Roles
    public enum UserRoles
    {
        Admin, // Represents the role of an administrator
        User // Represents the role of a regular user
    }
}
